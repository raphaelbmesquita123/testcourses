import styled from 'styled-components'

export const Container = styled.div`
    .helloContainer{
        background-color: var(--blue-500);
        p{
            display: block;
            padding: 2rem;
            text-align: center;
            color: var(--white);
        }
    }
`